void main(List<String> args) {
  String name = 'TREE';
  var address = '';

  address = name;
  // invalid assigning operation (cannot assign int to String)
  // const age = 20;
  // address = age;
  print(address);
}
