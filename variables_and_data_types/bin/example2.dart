void main(List<String> args) {
  // final age = [1, 2, 3];
  // age.removeAt(0);

  final age = 500;
  print(age);

  // this is also invalid
  // age = 50;

  // can we apply the same with const? (runtime exception)
}
